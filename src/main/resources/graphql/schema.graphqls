type Query {
    post(id: ID!): Post
    posts: [Post]
}

type User {
    id: ID!
    firstName: String
    lastName: String
}

type Post {
    id: ID!
    title: String!
    content: String
    writer: User
}